# Yahoo Finance 데이터 수집을 위한 Python 의존성

# Yahoo Finance 라이브러리
yfinance>=0.2.40

# MariaDB 연결
mysql-connector-python>=9.0.0

# 환경변수 로드 (.env 파일 지원)
python-dotenv>=1.0.0

# 데이터 처리 (yfinance가 내부적으로 사용)
pandas>=2.2.0
numpy>=1.26.0
